# Production dependencies for BigQuery Data Quality Monitoring
# Pin versions for reproducibility; bump deliberately, not accidentally.

google-cloud-bigquery==3.25.0
google-cloud-bigquery-storage==2.27.0
google-auth==2.35.0
pandas==2.2.3
pydantic==2.9.2
python-dotenv==1.0.1
pyyaml==6.0.2
tenacity==9.0.0
